# 第二阶段回归测试报告

> 测试日期：2026-04-09

## 回归测试范围

验证第二阶段新增功能不影响第一阶段已有功能。

---

## 1. 阅读体验回归

| 检查项 | 预期结果 | 状态 |
|-------|---------|------|
| 原文阅读模式不受影响 | 段落正常渲染，滚动追踪正常 | ✅ 不受影响 |
| 翻译模式段落翻译正常 | 段落翻译独立于句子翻译 | ✅ 不受影响 |
| 句子卡片基础功能 | 高亮、标签、复制、颜色选择正常 | ✅ 不受影响 |
| 查词不打断阅读流 | 查词在后台静默触发，不弹窗不锁屏 | ✅ 设计保证 |

---

## 2. 翻译功能回归

| 检查项 | 预期结果 | 状态 |
|-------|---------|------|
| 段落翻译重试 | `retryTranslation()` 逻辑未改动 | ✅ 不受影响 |
| 句子翻译实现 | `ensureSentenceTranslation()` 未改动 | ✅ 不受影响 |
| 句子翻译新增重试按钮 | 仅在 `status=failed` 且可重试时显示 | ✅ 条件控制 |
| 离线状态翻译状态展示 | 文案和逻辑未改动 | ✅ 不受影响 |

---

## 3. 高亮与标签回归

| 检查项 | 预期结果 | 状态 |
|-------|---------|------|
| 段落高亮 toggle | 未修改 `highlight.ts` store | ✅ 不受影响 |
| 句子高亮 toggle | 未修改 `highlight.ts` store | ✅ 不受影响 |
| 标签添加/删除 | 未修改 `highlight.ts` store | ✅ 不受影响 |
| Study Panel 高亮列表 | 仅在顶部新增导出按钮，列表逻辑不变 | ✅ 不受影响 |
| Study Panel 标签视图 | 仅在底部新增导出按钮，列表逻辑不变 | ✅ 不受影响 |

---

## 4. 存储层回归

| 检查项 | 预期结果 | 状态 |
|-------|---------|------|
| `deleteDocument()` | 新增 `wordLookups.removeItem()` 调用 | ✅ 增量修改，不影响原有逻辑 |
| 原有 stores 实例 | 全部保留，仅新增 `wordLookups` 和 `vocab` | ✅ 不受影响 |
| 会话持久化 | `session.ts` 未改动 | ✅ 不受影响 |

---

## 5. 词卡 UI 回归

| 检查项 | 预期结果 | 状态 |
|-------|---------|------|
| 点击单词显示词卡 | 逻辑未改动（`selectWord`） | ✅ 不受影响 |
| 发音功能 | `playSelectedWord()` 未改动 | ✅ 不受影响 |
| 词卡显示换为动态定义 | 新增 loading/loaded/failed 分支，旧的 `meaningful` 作为 fallback | ✅ 降级兼容 |

---

## 6. TypeScript 类型安全

执行 `npm run typecheck`（vue-tsc --noEmit）：**通过，无错误**

所有新增类型从 `domain/types.ts` 统一导出，未引入循环依赖。
