# 第一阶段开发日志

## 2026-03-18 My Study / Highlights & Tags 收敛与重构

### 需求收敛
- 复核第一阶段产品文档、执行记录、UI 说明与当前阅读页实现。
- 新增 `docs/执行记录/第一阶段/13-MyStudy与HighlightsTags功能收敛说明.md`。
- 明确 `My Study` 是全局沉淀入口，`Highlights & Tags` 是当前段落局部摘要区。

### 实现修改
- 重构 `src/pages/ReaderView.vue` 中的 `My Study`，让 `Highlights`、`Tags`、`Recently Read` 都成为真实入口。
- 新增全局 `My Study` 回看面板，分别承载全量高亮与 tag 视图。
- 收缩右侧与移动端详情中的 `Highlights & Tags`，只保留当前段落摘要与局部跳转。
- 补齐从列表 / tag / 当前段落摘要跳回正文的联动闭环。

### 验证
- `npm test` 通过。
- `npm run build` 通过。
- 已按收敛后的定义完成代码级回归检查。

## 2026-03-17

### Phase 0 文档与方案收敛

- 完整阅读第一阶段产品、设计、计划与执行记录文档。
- 收敛第一阶段能力边界，只保留阅读主链。
- 建立第一阶段执行记录文档体系。

### Phase 1 初始工程搭建

- 建立 Vue 3 + Vite + Pinia + Vue Router 基础结构。
- 建立文档库页面与阅读页入口。
- 接入 `pdfjs-dist`、`localforage`、`vite-plugin-pwa`。

## 2026-03-18

### Phase 1-2 双端阅读容器与主阅读流重构

- 重写 `src/pages/ReaderView.vue`。
- 重写 `src/pages/LibraryView.vue`。
- 新增 `src/styles.css`。
- 打通 PC 多区域阅读布局与移动端单列阅读流。
- 打通原文模式与译文模式。
- 将正文改为连续阅读流，不再使用展示型段落卡片列表。

### Phase 3 翻译主链重构

- 重写 `src/services/translation/index.ts`。
- 重写 `src/stores/reader.ts`。
- 实现当前段落与邻近段落的翻译触发。
- 实现翻译缓存命中、失败态与 `Retry`。

### Phase 4 高亮、标签、详情层落地

- 重写 `src/stores/highlight.ts`。
- 以段落和句子作为真实高亮对象。
- 以高亮对象作为 tag 的绑定载体。
- 让 PC 右侧详情面板与移动端底部详情弹层读取同一套数据。

### Phase 5 恢复与缓存离线落地

- 重写 `src/services/storage/index.ts`。
- 重写 `src/stores/session.ts`。
- 重写 `src/services/pdf/parse.ts`。
- 建立 `documents/files/parsed/translations/annotations/sessions` 六类本地持久化对象。
- 使用 `IntersectionObserver` 记录当前段落并持久化会话。
- 让缓存状态、离线状态、已保存状态反映真实持久化链路。

### 工程修正

- 新增 `src/domain/types.ts`。
- 清理文本标准化与翻译兜底中的编码问题。
- 修复阅读页中变量命名与浏览器 `document` 冲突的风险。
- 将 `package.json` 中的 `test` 入口改为可执行的类型校验脚本。

### 验证结果

- `npx vue-tsc --noEmit` 通过。
- `npm test` 通过。
- `npm run build` 于 2026-03-18 通过。

### 当前阅读页问题审阅与修复前测试

- 重新阅读第一阶段开发文档、执行记录文档、UI 文档和双端精修说明。
- 重新核对当前阅读页关键实现文件：`ReaderView.vue`、`reader.ts`、`highlight.ts`、`styles.css`。
- 对 `Structure`、`Tag`、`PC 侧边功能区跟随` 三项问题执行修复前需求对照测试。
- 新增测试记录文档 `docs/执行记录/第一阶段/12-当前阅读页问题测试记录.md`。
- 确认本轮修复顺序为：
  1. 先修 `Structure`
  2. 再修 `Tag`
  3. 最后修 PC 两侧功能区跟随与滚动模型

### 本轮修复前结论

- `Structure` 当前错误地显示为逐段正文预览列表。
- `Tag` 数据层真实存在，但缺少回看与跳回原文闭环。
- PC 阅读页当前的滚动模型使左右侧边区在长文阅读中逐渐失去可用性。

### 当前阅读页问题修复与回归

- 修复 `Structure`：改为按页聚合的降级结构导航，并保留点击跳转与当前项联动。
- 修复 `Tag`：补齐 `My Study > Tags` 的回看入口、tag 命中内容列表与跳回原文链路。
- 修复 PC 两侧功能区跟随：将左右侧栏改为 sticky 辅助区，并去掉 `Structure` 内层多余滚动。
- 执行修复后回归测试：`npm test` 通过。
- 将修复结果与回归结果写回 `docs/执行记录/第一阶段/12-当前阅读页问题测试记录.md`。

### 当前遗留

- 当前环境尚未执行浏览器自动化或真机手势测试。
- 未接入真实翻译服务时使用 glossary fallback，主链可运行但不代表最终线上翻译质量。
- 当前阅读页三项问题尚未修复，本轮进入正式修复前已经完成需求对照和记录落档。

## 2026-03-18 移动端专项梳理、开发与测试

### 需求收敛
- 完整复核 `docs/` 下第一阶段开发文档、执行记录、第二阶段边界文档、UI 设计说明和移动端设计稿。
- 新增 `docs/执行记录/第一阶段/14-移动端功能梳理与落地记录.md`，明确移动端第一阶段必须能力、当前差距、实现优先级与遗留边界。
- 确认本轮只补齐移动端第一阶段真实能力，不展开第二阶段的单词本、查词、导出和云同步。

### 实现修改
- 收口 `src/pages/ReaderView.vue` 中的移动端详情底部弹层，补齐“Current paragraph -> Translation status -> Highlights and tags”的清晰层级。
- 新增移动端当前段落状态说明文案，使“译文已缓存 / 未缓存 / 离线不可加载 / 可重试”更贴近真实缓存能力。
- 将底部状态区中的 `Offline` 表达改为基于真实状态派生：区分 `Offline ready`、`Offline partial`、`Offline limited`、`Offline unavailable`。
- 收口移动端句子选中后的临时操作条：执行 `Highlight`、`Add tag`、`Copy` 后自动退场，避免操作条误变成常驻工具栏。
- 继续压轻移动端恢复提示、底部状态区和临时操作层的尺寸与权重，让正文保持主角地位。

### 验证
- `npm test` 通过。
- `npm run build` 通过。
- 已按移动端专项目标完成代码级走查，确认未破坏 PC 既有阅读、详情和 My Study 主链。
